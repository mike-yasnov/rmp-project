ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.highloadinvest.gateway.ApplicationKt.module ]
    }
}

clickhouse {
    url = "jdbc:clickhouse://localhost:8123/default"
    url = ${?CLICKHOUSE_URL}
}

redis {
    host = "localhost"
    host = ${?REDIS_HOST}
    port = 6379
    port = ${?REDIS_PORT}
}
